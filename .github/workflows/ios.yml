name: iOS CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: |
        xcodebuild build -project refomo.xcodeproj \
          -scheme refomo \
          -destination 'generic/platform=iOS Simulator'

    - name: Run Tests
      run: |
        xcodebuild test -project refomo.xcodeproj \
          -scheme refomo \
          -sdk iphonesimulator \
          -destination 'platform=iOS Simulator,name=iPhone 16'
